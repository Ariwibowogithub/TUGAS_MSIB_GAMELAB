// Kode JavaScript kustom Anda bisa ditambahkan di sini

// Contoh JavaScript sederhana untuk menampilkan pesan di konsol
console.log("Hello, world!");

// Contoh JavaScript untuk manipulasi DOM
document.addEventListener("DOMContentLoaded", function() {
    // Fungsi ini akan dipanggil saat dokumen telah dimuat sepenuhnya
    console.log("Dokumen telah dimuat sepenuhnya.");

    // Contoh manipulasi DOM: Mengubah teks header
    const header = document.querySelector("header h1");
    header.textContent = "Selamat Datang di Gamelab";
});
